---
title: "[M5-06] npm publish pipeline (release-please + provenance)"
milestone: "M5 — Auth + Distribution + Smoke Tests"
labels: ["type:infra", "area:ci", "priority:P0-critical", "size:M"]
assignees: []
estimate: "5h"
---

## Summary

Publish `@ambitresearch/genie` and `@ambitresearch/genie-viewer` to
npm on every release-please-generated release. Enable `--provenance`
(npm supply-chain attestation). Use the Ambit Research-owned scope
`@ambitresearch`.

## Context

- INDEX.md: distribution = npm + `.mcpb` + Docker.
- Research report §7 step 1 + §5 prior art (Framelink uses `npx figma-developer-mcp`).
- M0-04 fallback (recorded in `CLAUDE.md` hard rule 7): the bare `genie` name
  and the `@genie` scope are owned by unrelated npm users. The server therefore
  publishes as **`@ambitresearch/genie`** and the viewer as
  **`@ambitresearch/genie-viewer`** under the Ambit Research-owned scope.
- npm provenance requires a public source repository. The release workflow
  fails before `npm publish` while this repository is private; repository
  visibility and the `NPM_TOKEN` secret are operator prerequisites for the
  first live release.

## Acceptance Criteria

- [x] AC1 — the release workflow's publish jobs fire deterministically off
      every release-please-created release. **Reconciled wording:**
      `release-please-config.json` sets `include-component-in-tag: true`, so
      tags are `server-vX.Y.Z` / `viewer-vX.Y.Z`, not a
      bare `v*`. `.github/workflows/release.yml` creates each release only
      after the merged release PR's CI completes successfully, then gates
      publish on release-please's per-package `release_created` output. This
      avoids a separate `on: push: tags: v*` workflow, which would not fire
      for tags created with `GITHUB_TOKEN`.
- [x] AC2 — Workflow publishes both packages with `--provenance` flag set.
- [x] AC3 — `NODE_AUTH_TOKEN` sourced from GitHub Actions `NPM_TOKEN`
      secret with publish scope.
- [x] AC4 — Server publishes as `@ambitresearch/genie` (scoped, public) — see the
      M0-04 fallback under Context.
- [x] AC5 — Viewer publishes as `@ambitresearch/genie-viewer` (scoped, public).
- [x] AC6 — Publish is completion-driven by the M0-04 `CI` workflow:
      `.github/workflows/release.yml` handles `workflow_run: completed` for
      `CI` on `main`, proceeds only for a successful `push` run, and preserves
      each main-push CI run from concurrency cancellation. Normal release PR
      updates use `skip-github-release`; after that PR merges, its own green CI
      completion runs release creation alone. The production jobs then require
      the package dry-run and `verify-release`, which proves each tag targets
      that same CI-tested SHA before publish.
- [x] AC7 — Release notes auto-generated by `release-please`.

## Implementation Notes

- File: `.github/workflows/release.yml`.
- `setup-node@v4` with `registry-url`, `always-auth: true`.

## Out of Scope

- Beta / canary channels (v2).

## Dependencies

- Blocks: M5-09 … M5-15 (harnesses install via npm).
- Blocked by: M0-04.

## Agent Workflow

**Follow the full SDLC in [`AGENTS.md`](../../../AGENTS.md)** (repo root): orient →
plan-on-issue → TDD (failing test first) → test against the local live service →
adversarial self-review → open PR → peer-AI review loop (iterate until zero open comments
and explicit approval) → merge → monitor CI → follow-up PR if CI breaks, else close.

Do not merge with open review comments. Do not leave `main` red. The issue closes only
when the PR is merged, the reviewer approved, CI is green, and every AC has evidence.

## Definition of Done

- [x] Tests added — `publish-dry-run` job in `release.yml` runs
      `npm publish --dry-run --provenance --access public` for both packages
      on every PR and successful main CI completion (non-production; never
      touches the real registry), exercising the production command shape.
- [x] Docs updated.
- [ ] Manual verification — dry-run publish + clean tarball install verified
      locally for both packages; real release publish and provenance
      verification remain pending repository visibility becoming public,
      `NPM_TOKEN` provisioning, and the first tagged release after merge.
- [x] No new ESLint/TS errors.
- [ ] Reviewed by 1 maintainer.

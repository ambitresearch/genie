/* Fixture kit vendored runtime stub (M4-02 test fixture).
   Exists so AC5 can assert `_vendor/` serves at `/_vendor/react.production.min.js`.
   Not a real React build — just a byte payload the dev server can hand back. */
globalThis.React = { __fixture: true };

{
  "name": "@genie/viewer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "genie preview viewer — Vite-backed UI-kit grid (M4). Multi-page dev config (M4-02): each components/**/preview.html is a Vite entry point.",
  "bin": {
    "genie-viewer": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx watch src/cli.ts",
    "serve": "vite",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "fast-glob": "^3.3.3",
    "open": "^11.0.0",
    "vite": "^8.1.3"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "author": "Roshan Gautam",
  "devDependencies": {
    "@types/node": "^26.0.0",
    "chokidar": "^3.6.0"
  }
}
